version: "2"
plugins:
  - name: golang
    wasm:
      url: file://../../bin/sqlc-gen-go.wasm
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "mysql"
    codegen:
    - plugin: golang
      out: db
      options:
        package: db
        sql_package: database/sql
        emit_json_tags: true
        query_parameter_limit: 2
